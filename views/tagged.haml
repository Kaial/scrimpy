%h1 Posts Tagged =tag
-posts.each do |post|
	.post
		%p.date
			=post.created.strftime("%b")
			%b =post.created.strftime("%d")
		%h2.title %a{:href=post.url} =post.title
		%p.meta %small =post.linked_tags
		.entry
			=post.summary_html
			-if post.more?
				%a{:href=post.url} Continue